<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#fff</item>
        <item name="colorPrimaryDark">#fff</item>
        <item name="colorAccent">#616161</item>
        <item name="android:textViewStyle">@style/MyTextViewStyle</item>
    </style>


    <!--自定义对话框样式-->
    <style name="dialog_style_bottom" parent="android:Animation">
        <!--进入时的动画-->
        <item name="android:windowEnterAnimation">@anim/dialog_bottom_enter</item>
        <!--退出时的动画-->
        <item name="android:windowExitAnimation">@anim/dialog_bottom_exit</item>
    </style>

    <style name="MyTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/googlesan</item>
    </style>


    <!--自定义对话框样式-->
    <style name="dialog_style_top" parent="android:Animation">
        <!--进入时的动画-->
        <item name="android:windowEnterAnimation">@anim/dialog_top_enter</item>
        <!--退出时的动画-->
        <item name="android:windowExitAnimation">@anim/dialog_top_exit</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
